/* Accounts config. Leave the values empty to disable login+sync entirely
   (the app then behaves exactly like v1.3 — fully offline, no account UI).
   The publishable/anon key is safe to expose in a client app; Row-Level
   Security in the database is what protects each user's data. */
window.VB_CONFIG = {
  supabaseUrl: "https://nwvohqnqtxlfxctjvbog.supabase.co",
  supabaseKey: "sb_publishable_wXL09ZE4mzqUFmSZvulJKA_N0l1YQZK"
};
